<div class="row">
  <% @packs.each do |pack| %>
    <div class="col-xs-12 col-lg-6">
      <div class="card shadow mb-4" id="<%= pack.id %>">
        <h5 class="card-header">
          <%= pack.name %>
          (<%= pack_cost(pack) %>)
        </h5>
        <div class="card-body card-table">
          <table class="table table-striped table-sm">
            <%= render 'cards/table_data', cards: pack.cards.sort_by(&:sort_id), skip_source: true, owned_cards: @owned_cards %>
          </table>
        </div>
      </div>
    </div>
  <% end %>
</div>
